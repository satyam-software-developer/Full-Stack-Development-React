Problem statement
Create and export a middleware function that logs changes to the console when the state is updated in the loggerMiddleware.js file.

Import and add this middleware inside the Redux store.
